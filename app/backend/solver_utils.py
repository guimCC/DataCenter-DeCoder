from main import Module
import json
import pandas
import pulp

def solve_module_list(modules: list[Module], specs: json, weigths):
    pass

def solve_module_placement(modules: list[Module], specs: json, weights, module_list) -> json:
    return json()